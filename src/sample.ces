/* Factorial en CEs */

sin_tipo main() {
    entero x;
    x = leer(); /* leer un entero */
    entero fact;
    fact = 1;
    si (x < 0) retorno;
    fact = fact * x;
    x = x - 1;
    mientras (x = 0) {
        fact = fact * x;
        x = x - 1;
    }
    salida(fact);
}